# 🏗️ 소프트웨어 아키텍처 패턴: Tetris 프로젝트 적용 사례

지금까지 테트리스 프로젝트(004 챕터)를 객체지향적으로 리팩토링하면서 자연스럽게 적용된 세 가지 핵심 소프트웨어 엔지니어링 개념을 정리합니다.

---

## 1. 관심사의 분리 (Separation of Concerns, SoC)

> [!NOTE]
> **정의:** 컴퓨터 프로그램을 여러 개의 구별된 부분으로 나누는 디자인 원칙입니다. 각 부분은 자신만의 명확한 '관심사(역할)'를 가지며, 다른 부분의 작업에 간섭하지 않습니다.

### 🎮 테트리스 적용 사례
이전 챕터에서는 키보드 입력, 블록 이동, 화면 출력이 하나의 거대한 덩어리로 섞여 있었습니다. 하지만 현재 구조에서는 관심사가 명확히 분리되었습니다.
*   **`GameCore` 클래스 (로직 관심사):** 게임의 핵심 규칙, 블록 이동, 충돌 검사, 점수 계산 등 **"수학적이고 논리적인 상태 변화"**만을 책임집니다. 화면에 어떻게 그려질지는 전혀 관심이 없습니다.
*   **외부 렌더러/UI (표현 관심사):** `GameCore`에서 계산된 결과물만 받아와서 콘솔 창이나 화면에 예쁘게 그리는 역할만 책임집니다. 게임 로직에는 일절 관여하지 않습니다.

### 🌟 이점
*   **유지보수성:** 버그가 렌더링 쪽인지 로직 쪽인지 명확히 알 수 있습니다.
*   **유연성:** 콘솔 화면(CLI)용으로 만든 테트리스 코드를 단 한 줄도 고치지 않고, 나중에 언리얼 엔진이나 웹 브라우저용 테트리스로 손쉽게 이식할 수 있습니다.

---

## 2. 데이터 전송 객체 (Data Transfer Object, DTO)

> [!NOTE]
> **정의:** 프로세스 간(혹은 계층 간) 데이터를 전달하는 객체입니다. 내부에 복잡한 로직이나 동작(Method)을 가지지 않고, 오직 '데이터를 담아 나르는 상자' 역할만 합니다.

### 🎮 테트리스 적용 사례
프로젝트의 **`LocalSnapshot` 구조체**가 완벽한 DTO의 역할을 수행합니다.
*   `GameCore`의 내부 구조(`std::array<std::array<char, 10>, 20> board_`)는 로직 처리에 최적화되어 있지만 외부에서 읽기에는 다소 복잡합니다.
*   따라서 외부 렌더러가 읽기 가장 편한 형태(`std::array<std::string, 20> board_rows`)로 데이터를 예쁘게 포장해서 전달하는 전용 택배 상자가 바로 `LocalSnapshot`입니다.

### 🌟 이점
*   **정보 은닉 보호:** `GameCore` 내부의 민감한 변수(`rng_` 랜덤 엔진 등)를 렌더러에게 노출하지 않고, 딱 화면에 그릴 정보만 선택해서 넘길 수 있습니다.
*   **형태 최적화:** 보내는 쪽(로직)과 받는 쪽(UI)이 각자 자신이 다루기 편한 데이터 구조를 유지할 수 있습니다.

---

## 3. 스냅샷 패턴 (Snapshot Pattern)

> [!NOTE]
> **정의:** 객체의 현재 상태를 사진(Snapshot) 찍듯이 그대로 캡처하여 저장하거나 전달하는 패턴입니다. (메멘토 패턴(Memento Pattern)의 일종으로 분류되기도 합니다.)

### 🎮 테트리스 적용 사례
`GameCore::snapshot()` 메서드가 이 패턴을 구현하고 있습니다.
*   게임 루프는 1초에도 수십 번씩 업데이트(`step()`)될 수 있습니다. 
*   화면을 그리는 렌더러가 `GameCore`의 내부 변수를 직접 참조하고 있다면, 화면을 그리는 도중에 블록이 떨어져 버려서 화면이 깨지거나 충돌(Data Race)이 발생할 수 있습니다.
*   대신 `snapshot()` 함수를 호출해 **그 순간의 게임 상태를 복사(값 복사)**하여 넘겨주면, 렌더러는 그 복사본 사진을 보며 여유롭게 화면을 그릴 수 있습니다.

### 🌟 이점
*   **스레드 안전성(Thread-Safety) 기반 마련:** 로직을 계산하는 스레드와 화면을 그리는 스레드를 완벽하게 분리하여 병렬 처리를 할 수 있는 안전한 구조가 완성됩니다.
*   **불변성(Immutability):** 한 번 생성된 스냅샷 객체는 값이 변하지 않으므로, 리플레이(Replay)를 만들거나 과거 상태로 되돌리기(Undo) 기능을 구현하기가 매우 쉬워집니다.
